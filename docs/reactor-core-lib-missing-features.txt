================================================================================
reactor-core-lib — missing / incomplete features (host cutover leftovers)
================================================================================
Date: 2026-07-17 (ownership inversion pass)
Host: reactor-revival
Lib version in use: reactor-core-lib@1.2.12 (npm)
Import alias: "reactor-core"


PURPOSE
-------
Host is a UI wrapper + bridge + test suite. Simulation lives in reactor-core-lib.
Bridge is mandatory (inactive paths throw).


HOST BRIDGE SURFACE
-------------------
  revival-session-bridge.js   — facade (tick / dispatch / prestige / reboot /
                                respecDoctrine / hardReset)
  core-state-projection.js    — snapshot → host state/tiles
  bridge-grid-sync.js         — layout hydrate on diverge; runtime host→session
                                OFF by default (project out after tick/ops)
  bridge-upgrades.js          — levels + affordance projection
  bridge-mechanics.js         — narrow sell overrides + session→host modifiers
  bridge-intents.js           — UI → session ops
  bridge-heat.js              — heat segment/flow (read-only)
  route-session-events.js     — events → FX/UI


DONE (physics / tick authority — do not re-open)
------------------------------------------------
- Tick / heat / pulse / caps / vent / sell / purchase go through session
- No host shouldMeltdown / _effectivePower/_effectiveHeat / applyLayoutCaps
- Blueprint diff/cost via session; clipToGrid from lib
- Meltdown evaluation in lib; host executeMeltdown is presentation only
- Tooltips use describeCellPulse / getHeatSegmentAt
- Neighbors via session.queryNeighbors
- Weave EP via calculateWeaveEp


DONE (P0 ownership inversion — 2026-07-17)
------------------------------------------
1) Grid mirror sync
   syncGridCheap default runtimeFromHost=false; tick + session ops + read paths
   do not stomp session heat/ticks when layouts match. Layout diverge still
   rebuilds via syncGridFromGame (hydrate). clearAllTiles clears session grid
   instead of syncGridFromGame stomp.

2) Economy load-before-mutate
   EconomyManager / ExoticParticleManager setters no longer loadEconomyFromHost.
   Prestige/reboot chrome trusts projectToGame (protium preserve stays host).
   loadEconomyFromHost remains for init / new-game / setDefaults hydrate only.
   Cheat EP uses session.creditExoticParticles + project.

3) Upgrade dual-write
   Upgrade.setLevel no longer pushes setUpgradeLevels. Hydrate via
   pushHostUpgradeLevelsForLoad on load/sanitize; runtime levels from
   session project / upgradePurchased events. Test harness hydrates after
   host setLevel for suite compatibility.


DONE (P1 host rule leftovers — 2026-07-17)
------------------------------------------
4) Doctrine respec — bridge.respecDoctrine (DEBIT_EP + setUpgradeLevels +
   project); game.respecDoctrine is thin wrapper.
5) Full reboot — runFullReboot → bridge.hardReset (session.reboot + zero
   economy + keep experimental levels only + project).
6) Prestige/reboot chrome — no EP rewrite / loadEconomyFromHost after bridge.
7) Part affordability — PartSet.check_affordability reads session
   getEconomySnapshot + getUpgradeLevel; unlock/doctrine stay host UI policy.


P2 — KEEP ON HOST (document, do not "fix into lib")
----------------------------------------------------
- executeMeltdown presentation (overlay, haptic, recovered blueprint, engine stop)
- Tile._clearMeltdownRecovery presentation (unpause / restart engine)
- engine handleComponentExplosion hull heat dump (lib emits event only)
- syncHostSellOverridesToSession when tests/UI force auto_sell / altered_max_power
- UnlockManager tier visibility rules (uses session placedCounts) — product UI policy
- valtio state, rAF engine shell, save chrome, lit templates, audio/PWA
- blueprint serialize/share-code / validatePasteResources (UI gating over session costs)
- Test helpers may call pushHostUpgradeLevelsForLoad / loadEconomyFromHost /
  syncGridFromGame explicitly after host mutations (not production paths)


P3 — DOMAIN SHELLS (in progress / ongoing)
------------------------------------------
Goal shape per domain type:
  Part / Upgrade / Tile / Reactor = projection DTOs + display helpers
  Game / Engine = wire UI intents → bridge + schedule project
  state.js = valtio store + UI-only flags; reboot/prestige next to bridge

Still thick but no longer authoritative for money/EP/levels/tick physics.
Further line-count shrink is optional polish, not a cutover blocker.


P3 REVIEW — 2026-07-17 (public/src ownership pass; canvas refresh)
------------------------------------------------------------------
Cutover blockers remain DONE. Soft place/heat leaks and P1 dual-write
footguns are CLOSED. Remaining work is size polish + lib gaps + test
harness split — not re-cutover.

Canvas: public-src-core-lib-review (Cursor canvases/)

VERDICT (deep review canvas)
  Authority cutover complete. Size cutover is not.
  ~25k non-generated public/src lines; UI/services are the wrapper and stay;
  shrink targets are domain (~3.7k) + state.js (~1k). Bridge (~1.5k) stays.

Host layer line budget (excl. generated; 2026-07-17 count):
  components+templates  ~12.1k  (UI — keep)
  services              ~3.3k   (audio/PWA/leaderboard — keep)
  domain                ~3.7k   (thick shells — shrink to DTOs)
  root (app/state/…)    ~2.7k   (state.js god-module)
  bridge                ~1.5k   (mandatory adapter — keep)
  infra (core/schema/…) ~2.5k   (host utilities — keep)

Domain line counts (approx):
  state.js 965 | game.js 547 | game-save.js 498 | objectives.js 454
  grid.js 446 | engine.js 430 | upgrade.js 373 | reactor.js 279
  part.js 276 | blueprint.js 186
  bridge facade revival-session-bridge.js ~750 (+ satellites)

Per-shell role today:
  game.js       — wire/facade; host managers (non-authoritative); intents → bridge
  part.js       — catalog DTO + affordability host loop (lib gap)
  upgrade.js    — levels from project/events; buy via bridge; host side effects
  grid.js       — Tile projection + session-first setPart/clearPart via bridge
  reactor.js    — snapshot stats + large modifier mirror + meltdown presentation
  engine.js     — rAF shell; bridge.processTick only; explosion FX + hull dump
  objectives.js — chapter/display chrome; claim/auto via bridge (check+claim)
  blueprint.js  — serialize/share + validatePasteResources UI gate
  game-save.js  — hydrate host then bridge (expected)
  state.js      — valtio + unlock tier policy + prestige/reboot chrome + schema barrel

SOFT LEAKS / P1 FOOTGUNS — ALL DONE (do not reopen as authority bugs)
  1) Tile.setPart / clearPart — DONE session-first
  2) Explosion hull dump — DONE session-authored
  3) Meltdown recovery heat — DONE session-authored
  4) EconomyManager / EP setters — DONE guarded (withHostEconomyHydrate /
     _isRestoringSave; VITEST allows; prod throws outside hydrate)
  5) Achievement dual-write — DONE notifyUnlock from session events;
     unlock() session-first then notice only
  6) Pre-tick syncReactorScalarsFromGame — DONE (vent/sell harness only)
  7) Objectives claim — DONE bridge.completeAndClaimObjective /
     tryAutoCompleteCurrentObjective (session checkObjective + claimCurrent);
     host markComplete+grantReward loops removed from claim/auto paths
  8) SET_TOGGLE / REBOOT intents — DONE UI producers via drain
  9) clearReactorHeat — DONE deleted
 10) Doctrine place gate — DONE single gate on bridge.placePart /
     placePartUnpaid (session isUpgradeAvailable via partset)

SIZE DEBT (optional polish — not spend authority)
  - Reactor modifier field farm (sessionModifiers + field copy for legacy readers)
  - state.js god-module (valtio + UnlockManager + reboot chrome + schema barrel)
  - ObjectiveManager / AchievementManager display + chapter chrome (thick)
  - Control-deck auto-sell/vent / Stirling / overflow host-projected fields
  - buildPartSummary / sell-modal part.cost fallbacks
  - PartSet.check_affordability host loop (blocked on lib afford flags)
  - syncReactorScalarsFromGame retained for test harness host heat writes

RANKED REMAINING (S = size, L = lib, T = tests)
  S1) Collapse Reactor modifier farm → thin DTO from projectModifiers/snapshot
  S2) Slim state.js (valtio + UnlockManager + bridge-adjacent reboot)
  S3) Thin ObjectiveManager / AchievementManager to display + event chrome
  S4) Control-deck / overflow / Stirling from session snapshot fields only
  L1) Lib CLAIM_OBJECTIVE first-class op — host wrap fine until then
  L2) Lib part afford snapshot flags (filterAffordablePlacements only today)
  T1) Test harness: createGameSession / headless for sim; host tests for
      projection + UI policy only

TEST HARNESS NOTE (structural; not authority)
  Suites boot the full host and hydrate after host mutations
  (syncGridFromGame / loadEconomyFromHost / pushHostUpgradeLevelsForLoad /
  syncReactorScalarsFromGame for host heat/power writes before vent/sell).
  A lib-centric suite would prefer createGameSession / createHeadlessRunner
  for pure sim and host tests only for projection + UI policy — larger than
  shell shrink; not required for authority.

BRIDGE GAPS (remaining)
  - Full deletion of reactor modifier field farm (compat copy remains)
  - Part afford snapshot flags (lib)
  - First-class CLAIM_OBJECTIVE command (lib; host bridge wraps check+claim)
  - Optional HUD display-rate snapshot fields (lib)

NOT GAPS (correct)
  sell/vent/purchase/place/blueprint/prestige/reboot/respec/cheat EP
  caps/stats from snapshot; neighbors/pulse/heat segments from session
  objective claim + achievement unlock via session (host is chrome only)
  SET_TOGGLE / REBOOT intent producers


reactor-core IMPORT SURFACE (host today)
----------------------------------------
  revival-session-bridge.js  createGameSession, computeBlueprintDiff,
                             computePartSellValue
  state.js                   calculateWeaveEp
  domain/blueprint.js        clipToGrid
  (most session APIs used via bridge methods, not direct imports)


SUCCESS CRITERIA (updated)
--------------------------
1. Host never evaluates meltdown/hull locally.                    DONE
2. Host never spends money/EP or bumps upgrade levels except via
   commands (respec/hardReset/purchase go through session).       DONE
3. Host never assigns grid.maxPower/maxHeat.                     DONE
4. Host never writes cell _effectivePower/_effectiveHeat.        DONE
5. Manual vent and sell go through VENT_HEAT / SELL_POWER.       DONE
6. Bridge = intent → dispatch/tick → project on hot paths;
   hydrate on load/setup; tick/_syncForObjectiveEval may also
   push host runtime/economy into session when tests/UI mutate
   host mirrors first (protium/sell/heat harness paths).         DONE

HOST SYNC HARDENING (2026-07-17 evening — keep)
-----------------------------------------------
- processTick copies session protiumParticles to host before
  post-tick loadEconomyFromHost can clobber mid-flight gains
- ObjectiveManager clones bundled objective defs (no shared
  completed flags across games); check_current_objective syncs
  grid + session.checkObjective; teardown clears auto-complete timer
- syncGridToGame applies/clears parts even on exploding tiles so
  explosion clear + re-place do not diverge host vs session
- Part.base_power reads catalog basePower (not shop power)
- Upgrade.setLevel merges one level into session (setUpgradeLevels
  replace-all safe); test harness may still hydrate after setLevel

NOT DONE (do not claim as finished)
-----------------------------------
- Net frontend line cut is not ~800–1000 in this tree (recent
  domain/bridge pass is roughly net-flat; objectives thinned most)
- ObjectiveManager still has start() setTimeout auto-complete and
  claim feedback timers — not passive objectiveComplete-only
- EconomyManager / TimeKeeper / SessionManager still exist in game.js
- Tile still has heat_contained / addHeat / ticks (projection +
  harness writes); reactor meltdown presentation remains on host
- engine.js rAF / offline catchup and blueprint share codec still
  host-owned (optional next shrink targets, not authority blockers)

End of document.
