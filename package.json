{
  "name": "reactor-game-tests",
  "version": "1.0.0",
  "description": "Tests for Reactor Game",
  "main": "index.html",
  "scripts": {
    "test": "vitest",
    "generate-version": "node scripts/generate-version.js",
    "dev": "npm run generate-version && npx live-server --port=8080 --open=/",  
    "start": "npx live-server --port=8080 --open=/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^1.6.1"
  }
} 