================================================================================
reactor-core-lib — missing / incomplete features (host cutover leftovers)
================================================================================
Date: 2026-07-16
Host: reactor-revival
Lib version in use: reactor-core-lib@1.2.12 (npm)
Published: v1.2.12 (commit 563a366)


PURPOSE
-------
Host is a UI wrapper + bridge + test suite. Simulation lives in reactor-core-lib.
Bridge is mandatory (inactive paths throw).


HOST BRIDGE SURFACE
-------------------
  revival-session-bridge.js   — facade
  core-state-projection.js    — snapshot → host state/tiles
  bridge-grid-sync.js         — layout/heat/ticks + placedCounts
  bridge-upgrades.js          — levels + affordance
  bridge-mechanics.js         — narrow sell overrides + session→host modifiers
  bridge-intents.js           — UI → session ops
  bridge-parts.js             — revision bump only
  bridge-heat.js              — heat segment/flow
  route-session-events.js     — events → FX/UI


DONE ON HOST (recent)
---------------------
- PartSet / UpgradeSet from session catalogs
- Neighbors / displayTitle / prestige applyPrestige via session
- P1–P5 host polish (active_* shim, dead grid renderers, tooltip peel,
  IC2 classification move, upgrade hydrate collapse)
- Dead: integrityMap, syncHeatFrom/ToTiles, clearAllParts
- Dead: HEAT_EPSILON, MELTDOWN_HEAT_MULTIPLIER, traits.js
- session_ep_weave derive → lib calculateWeaveEp
- Valve transferMultiplier + reflector neighborPulseValue from compiled
  catalog (host no longer multiplies valve transfer)
- Dual-path freeze: no full per-tick mechanicsOverrides / grace stomp;
  grace set once on bridge init; meltdown presentation no longer writes
  hull/failure; vent residual sold_heat host policy removed;
  upgrade hydrate load/init only; purchase projects with skipSessionSync


STILL OPEN — NEED LIB
---------------------
  (none)


STILL OPEN — HOST POLISH (UI wrapper target)
--------------------------------------------
  (none)


DONE — HOST POLISH
------------------
H1  Retargeted tests off tileset.getTilesInRange; deleted host helper
H2  Retargeted partset tests to getPartsByLevel; deleted getPartsByTier alias
H3  Retargeted pulse tests to session.describeCellPulse; deleted helper
H4  Deleted unused components/reactor-classification.js
H5  Removed unused Part shell traits / trait_mask (UI never read them)
H6  Deleted domain/balance.js BALANCE mirror; tests use VALVE_TOPUP_THRESHOLD
    from SIM_CONSTANTS (constants/sim.js)


HOST-ONLY (keep)
----------------
- Meltdown presentation, doctrine chrome, valtio/DOM/rAF
- Visual FX, Tile canvas, blueprint share-code UI
- Objectives/achievements UI orchestration
- UnlockManager tier rules (uses session placedCounts)
- Tooltip HTML + stirling segment + neighbor bonus attribution
- Engine explosion FX + hull heat dump on componentExplosion (lib emits
  event only; host adds tile heat to reactor for presentation)
- Narrow tick sell overrides (autoSellPercent / alteredMaxPower /
  powerToHeatRatio) when host test/UI values differ from session


FAT FILE MAP (approx lines)
---------------------------
  ui-* / services* / templates     — UI (keep)
  state.js (~1k)                   — valtio + unlock + reboot chrome
  domain/grid.js (~500)            — tile mirror + heatMap SoA
  bridge/revival-session-bridge.js — keep
  domain/game-save.js              — persistence chrome
  domain/engine.js                 — rAF/catchup/visual rings; tick → bridge
  domain/upgrade.js / part.js      — catalog shells
  domain/reactor.js                — stats projection + meltdown presentation


SUGGESTED NEXT
--------------
  1. Keep bridge shrinking toward intent → dispatch/tick → project
  2. Peel remaining tooltip display formulas onto session describe APIs
  3. Collapse Tile.setPart test helpers onto session dispatch (optional)

End of document.
